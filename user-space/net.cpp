#include "command.h"
#include "util.h"

/**
 * 在这里面构造netlink协议包,发送到内核
 */

int net_policy_insert(const net_policy_t &policy) { return 0; }
int net_policy_delete(policy_id_t id) { return 0; }

int enable_net_protect() { return 0; }
int disable_net_protect() { return 0; }
